### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Album {
  artists: [Artist]
  bands: [Band]
  genres: [Genre]
  id: ID!
  image: String
  name: String
  released: Int
  tracks: [Track]
}

type Artist {
  bands: [Band]
  birthDate: String
  birthPlace: String
  country: String
  firstName: String
  id: ID!
  instruments: [String]
  middleName: String
  secondName: String
}

type Band {
  genres: [Genre]
  id: ID!
  members: [Member]
  name: String
  origin: String
  website: String
}

type Genre {
  country: String
  description: String
  id: ID!
  name: String
  year: Int
}

type Member {
  id: ID!
  instrument: String
}

type Mutation {
  createTrack(albumId: ID, artistsIds: [ID], bandsIds: [ID], duration: Int, genresIds: [ID], released: Int, title: String!): Track
  deleteTrack(id: ID!): String
  updateTrack(albumId: ID, artistsIds: [ID], bandsIds: [ID], duration: Int, genresIds: [ID], id: ID!, released: Int, title: String!): Track
}

type Query {
  track: Track
  tracks(id: ID!): [Track]
}

type Track {
  album: Album
  artists: [Artist]
  bands: [Band]
  duration: Int
  genres: [Genre]
  id: ID!
  released: Int
  title: String!
}
