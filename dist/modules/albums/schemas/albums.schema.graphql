### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Album {
  artists: [Artist]
  bands: [Band]
  genres: [Genre]
  id: ID!
  image: String
  name: String
  released: Int
  tracks: [Track]
}

type Artist {
  bands: [Band]
  birthDate: String
  birthPlace: String
  country: String
  firstName: String
  id: ID!
  instruments: [String]
  middleName: String
  secondName: String
}

type Band {
  genres: [Genre]
  id: ID!
  members: [Member]
  name: String
  origin: String
  website: String
}

type Genre {
  country: String
  description: String
  id: ID!
  name: String
  year: Int
}

type Member {
  id: ID!
  instrument: String
}

type Mutation {
  createAlbum(artistsIds: [ID], bandsIds: [ID], genresIds: [ID], image: String, name: String!, released: Int, trackIds: [ID]): Album
  deleteAlbum(id: ID!): String
  updateAlbum(artistsIds: [ID], bandsIds: [ID], genresIds: [ID], id: ID!, image: String, name: String, released: Int, trackIds: [ID]): Album
}

type Query {
  album(id: ID!): [Album]
  albums: Album
}

type Track {
  album: Album
  artists: [Artist]
  bands: [Band]
  duration: Int
  genres: [Genre]
  id: ID!
  released: Int
  title: String!
}
