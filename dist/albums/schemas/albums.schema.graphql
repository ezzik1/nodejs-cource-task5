### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Album {
  artists: [Artist]
  bands: [Band]
  genres: [Genre]
  id: ID!
  image: String
  name: String
  released: Int
  tracks: [Track]
}

type Artist {
  bands: [Band]
  birthDate: String
  birthPlace: String
  country: String
  firstName: String
  id: ID!
  instruments: [String]
  middleName: String
  secondName: String
}

type Band {
  genres: [Genre]
  id: ID!
  members: [String]
  name: String
  origin: String
  website: String
}

type Genre {
  country: String
  description: String
  id: ID!
  name: String
  year: Int
}

type Mutation {
  createAlbum(artistsId: [ID], bandsId: [ID], genresId: [ID], image: String, name: String!, released: Int, trackId: [ID]): Album
  deleteAlbum(id: ID!): String
  updateAlbum(artistsId: [ID], bandsId: [ID], genresId: [ID], id: ID!, image: String, name: String, released: Int, trackId: [ID]): Album
}

type Query {
  album(id: ID!): Album
  albums: [Album!]
}

type Track {
  album: Album
  artists: [Artist]
  bands: [Band]
  duration: Int
  genres: [Genre]
  id: ID!
  released: Int
  title: String!
}
